@model Gestion_Soutenances.Models.PFE_Etudiant

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-center">Details</h1>

<div class="container">
    <h4 class="mt-4 mb-3">PFE_Etudiant</h4>
    <hr />

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">PFE_Etudiant</h3>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-2">
                    <strong>@Html.DisplayNameFor(model => model.Etudiant)</strong>
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Etudiant.Nom)
                </dd>

                <dt class="col-sm-2 mt-3">
                    <strong>@Html.DisplayNameFor(model => model.PFE)</strong>
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.PFE.desc)
                </dd>
            </dl>
        </div>

        <div class="card-footer">
            <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.ID">Edit</a>
            <a class="btn btn-secondary" asp-action="Index">Back to List</a>
        </div>
    </div>
</div>
